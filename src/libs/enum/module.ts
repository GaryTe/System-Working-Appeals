export enum HttpMethod {
  Get = 'get',
  Post = 'post',
  Delete = 'delete',
  Patch = 'patch',
  Put = 'put',
}

export enum StatusAppeal {
  New = 'new',
  'In work' = 'in work',
  Completed = 'completed',
  Canceled = 'canceled'
}
