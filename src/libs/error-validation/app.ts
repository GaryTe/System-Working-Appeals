export const EnvValidationMessageForApp = {
  host: 'Specify the environment variable HOST in the file .env.',
  port: 'Specify the environment variable PORT in the file .env. The min value port 1000, max value port 9000.',
  postgresNameDatabase: 'Specify the environment variable POSTGRES_DB in the file .env.',
  postgresUserName: 'Specify the environment variable POSTGRES_USER in the file .env.',
  postgresPassword: 'Specify the environment variable POSTGRES_PASSWORD in the file .env.',
  postgresEmail: 'Specify the environment variable PGADMIN_EMAIL in the file .env.',
  postgresPort: 'Specify the environment variable POSTGRES_PORT in the file .env. The min value port 1000, max value port 9000.',
  fakesmtpPort: 'Specify the environment variable SMTP_PORT in the file .env. The min value port 1000, max value port 9000.',
};
